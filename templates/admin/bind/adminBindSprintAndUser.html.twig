{% extends "base.html.twig" %}

{% block contents %}
    <p>
        <a href="{{ path('app_admin') }}">go to admin manager</a>
    </p>

    {{ form_start(form) }}
    {{ form_row(form.plainSprint) }}
    {{ form_row(form.plainUser) }}
    <input type="submit" value="bind together">
    {{ form_end(form) }}

    <br><br>
    <h5>Existing sprints with their users</h5>
    {% if sprints is empty %}
        <p>(empty set)</p>
    {% endif %}
    <ol>
        {% for sprint in sprints %}
            <li>{{ sprint.name }}</li>
            <ul>
                {% for user in sprint.users %}
                    <li>{{ user.username }}</li>
                {% endfor %}
            </ul>
        {% endfor %}
    </ol>
{% endblock %}